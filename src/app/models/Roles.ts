export class Roles {
    constructor(
      subscriber?: boolean,
      editor?: boolean,
      admin?: boolean
    ) {
    }
}


// export interface Roles {
//     subscriber?: boolean;
//     editor?: boolean;
//     admin?: boolean;
// }
